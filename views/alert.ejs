<%- include("headtable") %>

<body>
    <div class="container">
        <h1>Notify Customer</h1>
        <div class="table-container">
            <table class="table table-striped table-bordered">
                <thead>
                    <tr>
                        <th>First Name</th>
                        <th>Last Name</th>
                        <th>Phone</th>
                        <th>Email</th>
                        <th>Date</th>
                        <th>Alert</th>
                    </tr>
                </thead>
                <tbody>
                    <% data.forEach((item) => { %>
                    <tr>
                        <td><%= item.first_name %></td>
                        <td><%= item.last_name %></td>
                        <td><%= item.phone %></td>
                        <td><%= item.date %></td> <!-- เวลานัดหมอ -->
                        <td><button type="button" class="btn btn-primary" onclick="alert()">Alert</button></td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>
    <script>
        function alert(){
            
        }
    </script>
</body>
</html>